# Notes for agents working in this repo

- `workflows` at the repo root is a symlink to `.github/workflows`, kept for
  convenience (shorter paths). It is not a stray duplicate directory — both
  paths point to the same files, so edit through either one.
